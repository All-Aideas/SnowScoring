// Copy this file to config.js and replace with your own key.
// config.js is gitignored — never commit your real key.
// In production, this key should also be restricted by HTTP referrer in GCP.
window.SNOWSCORE_CONFIG = {
  GEMINI_API_KEY: "YOUR_GEMINI_API_KEY_HERE"
};
